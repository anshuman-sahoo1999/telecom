# NeonDB / PostgreSQL Database Connection (Set this for NeonDB cloud DB)
DATABASE_URL=postgres://username:password@ep-xyz.aws.neon.tech/neondb?sslmode=require

# Alternative Local MySQL Configuration (Fallback)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=telecom_db

# Server Configuration
PORT=5000
